
use yv;
CREATE TABLE yyzzz_nj(`city` VARCHAR(40) NOT NULL,
                    `year` INT,
                    `kind` VARCHAR(100) NOT NULL,
                    `num` INT
                    );
select * from yyzzz_nj LIMIT 0, 2000